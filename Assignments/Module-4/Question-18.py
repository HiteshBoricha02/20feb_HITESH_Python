# What happens when „1‟== 1 is executed?


print('''In Python, the == operator checks whether the values of the two operands are equal and also considers their types. 
         If the types are different, the comparison returns False.''')

result = "1" == 1
print(result) #out put is false